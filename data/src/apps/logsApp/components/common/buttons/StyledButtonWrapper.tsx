import styled from "styled-components";

// language=LESS
export const StyledButtonWrapper = styled.div`
  & {
    user-select: none;
    cursor: pointer;
    background: #fafafa;
    padding: 3px;
    display: inline-block;
    font-weight: normal;
    border: 1px solid rgba(83, 82, 82, 0.54);
    flex: 0;
  }
`;
