import styled from "styled-components";

export const LogTitle = styled.div`
  user-select: none;
  cursor: pointer;
  background: #eee;
  padding: 5px;
  flex: 1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;
